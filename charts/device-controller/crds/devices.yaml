---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: devices.teknoir.org
spec:
  group: teknoir.org
  names:
    kind: Device
    listKind: DeviceList
    plural: devices
    singular: device
  scope: Namespaced
  versions:
    - name: v1
      schema:
        openAPIV3Schema:
          description: Device is the Schema for the devices API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: DeviceSpec defines the desired state of Device
              properties:
                applications:
                  description: |-
                    Applications holds additional k8s resources to deploy to the device,
                    sourced from Git repositories following a GitOps model.
                  items:
                    description: |-
                      GitOpsApplication describes a set of k8s resources to deploy to the device
                      that are sourced from a Git repository following a GitOps model.
                    properties:
                      path:
                        description: |-
                          Path is the path within the repository to the YAML file (or directory)
                          containing the k8s resources to deploy to the device.
                        type: string
                      repo_url:
                        description: RepoURL is the URL of the Git repository that holds
                          the k8s resources.
                        type: string
                      revision:
                        description: Revision is the Git revision (branch, tag or commit)
                          to deploy.
                        type: string
                    required:
                      - repo_url
                    type: object
                  type: array
                device_type:
                  description: |-
                    INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                    Important: Run "make" to regenerate code after modifying this file
                  type: string
                keys:
                  description: |-
                    Secret holds secret data of a certain type. The total bytes of the values in
                    the Data field must be less than MaxSecretSize bytes.
                  properties:
                    apiVersion:
                      description: |-
                        APIVersion defines the versioned schema of this representation of an object.
                        Servers should convert recognized schemas to the latest internal value, and
                        may reject unrecognized values.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                      type: string
                    data:
                      additionalProperties:
                        format: byte
                        type: string
                      description: |-
                        Data contains the secret data. Each key must consist of alphanumeric
                        characters, '-', '_' or '.'. The serialized form of the secret data is a
                        base64 encoded string, representing the arbitrary (possibly non-string)
                        data value here. Described in https://tools.ietf.org/html/rfc4648#section-4
                      type: object
                    immutable:
                      description: |-
                        Immutable, if set to true, ensures that data stored in the Secret cannot
                        be updated (only object metadata can be modified).
                        If not set to true, the field can be modified at any time.
                        Defaulted to nil.
                      type: boolean
                    kind:
                      description: |-
                        Kind is a string value representing the REST resource this object represents.
                        Servers may infer this from the endpoint the client submits requests to.
                        Cannot be updated.
                        In CamelCase.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    metadata:
                      description: |-
                        Standard object's metadata.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
                      type: object
                    stringData:
                      additionalProperties:
                        type: string
                      description: |-
                        stringData allows specifying non-binary secret data in string form.
                        It is provided as a write-only input field for convenience.
                        All keys and values are merged into the data field on write, overwriting any existing values.
                        The stringData field is never output when reading from the API.
                      type: object
                    type:
                      description: |-
                        Used to facilitate programmatic handling of secret data.
                        More info: https://kubernetes.io/docs/concepts/configuration/secret/#secret-types
                      type: string
                  type: object
                manifest:
                  properties:
                    apps:
                      additionalProperties:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                plugins:
                  items:
                    properties:
                      apiVersion:
                        description: |-
                          APIVersion defines the versioned schema of this representation of an object.
                          Servers should convert recognized schemas to the latest internal value, and
                          may reject unrecognized values.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                        type: string
                      kind:
                        description: |-
                          Kind is a string value representing the REST resource this object represents.
                          Servers may infer this from the endpoint the client submits requests to.
                          Cannot be updated.
                          In CamelCase.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                        type: string
                      spec:
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  type: array
              required:
                - keys
                - manifest
              type: object
            status:
              description: SubResourcesStatus defines the observed state of Device
              properties:
                authorized_keys:
                  description: |-
                    INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                    Important: Run "make" to regenerate code after modifying this file
                  properties:
                    active:
                      type: boolean
                    key:
                      type: string
                  type: object
                build_image:
                  properties:
                    artifacts:
                      items:
                        type: string
                      type: array
                    triggered:
                      type: boolean
                  type: object
                build_installer:
                  properties:
                    artifacts:
                      items:
                        type: string
                      type: array
                    triggered:
                      type: boolean
                  type: object
                config:
                  properties:
                    active_config_hash:
                      type: string
                    github_rate_limits:
                      description: |-
                        GitHubRateLimits records, per GitHub API rate-limit bucket, that a GitOps
                        sync recently hit that bucket's limit. GitHub tracks rate limits
                        independently per credential (a personal access token, or a specific
                        GitHub App installation) and, for unauthenticated access, per outbound
                        IP shared across the whole cluster - so a limit hit on one bucket (most
                        likely unauthenticated access, given its much lower 60 requests/hour
                        budget) must not block GitOps syncs that would use a different,
                        unaffected bucket. While a bucket's entry is present and unexpired,
                        GitOps fetching that would use it is skipped until its RetryAfter has
                        passed, instead of retrying every reconcile and burning through the
                        rest of that bucket's window.
                      items:
                        description: |-
                          GitHubRateLimitStatus records an in-progress backoff after a GitOps sync hit
                          a GitHub API rate limit (primary or secondary/abuse) on a specific bucket.
                        properties:
                          bucket:
                            description: |-
                              Bucket identifies which independent GitHub rate limit budget this
                              backoff record applies to, e.g. "unauthenticated", "token", or
                              "app-install-<installation id>".
                            type: string
                          reason:
                            description: Reason is the error returned by GitHub that
                              triggered the backoff.
                            type: string
                          retry_after:
                            description: |-
                              RetryAfter is the earliest time at which GitOps fetching using this
                              bucket should be attempted again, taken from GitHub's own rate limit
                              reset (or Retry-After) signal.
                            format: date-time
                            type: string
                        required:
                          - bucket
                          - retry_after
                        type: object
                      type: array
                    gitops_applications:
                      description: |-
                        GitOpsApplications records, per GitOps application, the short Git commit
                        hash that was last loaded. It lets subsequent reconciles skip fetching
                        applications whose commit has not changed, reducing GitHub API usage.
                      items:
                        description: |-
                          GitOpsApplicationConfig records the last-loaded short Git commit hash for a
                          single GitOps application, keyed by its repository, revision and path.
                        properties:
                          commit:
                            description: |-
                              Commit is the short Git commit hash that was last loaded for this
                              application.
                            type: string
                          etag:
                            description: |-
                              ETag is the HTTP entity tag returned by GitHub for this application's
                              ListCommits request. It is sent back as an If-None-Match header on the
                              next reconcile so that an unchanged application yields a 304 Not Modified
                              response, which does not count against the GitHub API rate limit.
                            type: string
                          path:
                            type: string
                          repo_url:
                            type: string
                          revision:
                            type: string
                        type: object
                      type: array
                    last_config_change:
                      format: date-time
                      type: string
                  type: object
                encryption_key_gen_info:
                  properties:
                    active:
                      type: boolean
                    key_gen_success:
                      type: boolean
                  type: object
                remote_access:
                  properties:
                    active:
                      type: boolean
                    last_applied_tunnels:
                      items:
                        properties:
                          host:
                            type: string
                          name:
                            type: string
                          port:
                            type: string
                          type:
                            description: Type defines network protocols supported tunnels.
                            type: string
                        required:
                          - host
                          - name
                          - port
                          - type
                        type: object
                      type: array
                    port:
                      type: string
                    tunnels:
                      items:
                        properties:
                          host:
                            type: string
                          name:
                            type: string
                          port:
                            type: string
                          type:
                            description: Type defines network protocols supported tunnels.
                            type: string
                        required:
                          - host
                          - name
                          - port
                          - type
                        type: object
                      type: array
                  type: object
                state:
                  properties:
                    alerts:
                      items:
                        properties:
                          count:
                            type: integer
                          name:
                            type: string
                          state:
                            type: string
                        type: object
                      type: array
                    apps:
                      additionalProperties:
                        properties:
                          alerts:
                            items:
                              properties:
                                count:
                                  type: integer
                                name:
                                  type: string
                                state:
                                  type: string
                              type: object
                            type: array
                          cpu_usage:
                            type: number
                          cpu_usage_pct:
                            type: number
                          memory_usage:
                            type: number
                          memory_usage_pct:
                            type: number
                          ready_containers:
                            type: integer
                          restarts:
                            type: integer
                          running:
                            type: boolean
                          total_containers:
                            type: integer
                        type: object
                      type: object
                    cpu_usage:
                      type: number
                    memory_usage:
                      type: number
                    net_rx_rate:
                      type: number
                    net_tx_rate:
                      type: number
                    storage_usage:
                      type: number
                  type: object
              type: object
          type: object
          x-kubernetes-preserve-unknown-fields: true
      served: true
      storage: true
      subresources:
        status: { }
